# 基础排版验证

这是一段普通正文，用来检查段落间距、行高和字体是否正常。

这是第二段，包含 **粗体**、*斜体* 和 `inline code`。

## 列表

- 无序列表一
- 无序列表二
- 无序列表三

1. 有序列表一
2. 有序列表二
3. 有序列表三

## 引用

> 这是一段普通引用。
>
> 第二行引用内容，用于检查 blockquote 样式。

## 代码块

```ts
function greet(name: string) {
  return `hello, ${name}`;
}
```

## 表格

| 项目 | 说明 | 状态 |
| --- | --- | --- |
| 标题 | 检查字号层级 | 待验证 |
| 表格 | 检查边框和内边距 | 待验证 |
| 代码 | 检查代码块可读性 | 待验证 |

---

收尾段落，用来检查分割线之后的正文间距。
